---
title: "Antideploy REST API: Overview"
description: "Base URL, the two credential types, the endpoint surface, rate limits, and error format for the Antideploy API."
---

The Antideploy API creates a project, deploys it, and reports on it. It is
deliberately small, because that is all deploying takes.

## Base URL

```
https://antideploy.com/api/v1
```

<Tip>
That URL is also a live, unauthenticated description of the API. `curl` it and
you get the full contract as JSON: request shapes, error codes, and current
limits. If you are pointing an AI agent at Antideploy, give it
`https://antideploy.com/llms.txt` and let it find the rest — it needs no
credential from you at all.
</Tip>

## Authentication

Every request carries a bearer token:

```bash
curl https://antideploy.com/api/v1/deployments/$TASK_ID \
  -H "Authorization: Bearer $ANTIDEPLOY_KEY"
```

There are two kinds. A **project key** (`ad_`) is scoped to a single
application and identifies it, so no application id is ever sent. An **account
token** (`adu_`) represents you, can create applications, and therefore has to
name the one it is acting on with `?applicationId=<id>`.

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/api/authentication">
    Both credentials, what each can do, rotating and revoking.
  </Card>
  <Card title="Terminal login" icon="terminal" href="/api/terminal-login">
    How an agent gets an account token without a browser.
  </Card>
</CardGroup>

## Endpoints

| Method | Path | Purpose |
| --- | --- | --- |
| `GET` | `/api/v1` | The API contract. No credential required. |
| `POST` | `/api/v1/device/code` | Begin a terminal login. No credential required. |
| `POST` | `/api/v1/device/token` | Poll it, and collect the account token. |
| `POST` | `/api/v1/applications` | Create a project. Account tokens only. |
| `GET` | `/api/v1/applications` | List your projects. |
| `POST` | `/api/v1/deploy` | Push a project and deploy it. |
| `GET` | `/api/v1/deployments/{taskId}` | Status, steps, spec, warnings, hazards. |
| `PUT` | `/api/v1/secrets` | Write environment variables. |
| `GET` | `/api/v1/secrets` | List variable names. Never values. |

That is the complete surface. Deleting an application is deliberately not on
it: that needs the dashboard, so no credential sitting in a project directory
can destroy one.

<Card title="Deploy over the API" icon="rocket" href="/api/deploy">
  The full deploy reference, with the one command that ships a directory.
</Card>

## Request format

`POST /api/v1/deploy` is **`multipart/form-data`**: it carries your source
tree. Everything else is JSON.

## Rate limits

| Limit | Value |
| --- | --- |
| Deploys per hour, per application | 20 |
| Concurrent deploys per application | 1 |

A second deploy while one is running returns `409` with the in-flight
`taskId`, not an error you need to retry blindly. Exceeding the hourly limit
returns `429` with `retryAfterSeconds`.

<Note>
Both limits exist because agents retry. A human clicks Deploy a few times a
day; a loop that treats failure as retryable will hit the API as fast as it is
accepted.
</Note>

## Errors

Errors return a JSON body with a stable `code`, a human-readable `error`, and
a `documentation` URL pointing back at the contract.

```json
{
  "error": "Received 1 file but 2 paths. Send one `paths` field per `files` field, in the same order, or send an `archive` instead.",
  "code": "paths_mismatch",
  "documentation": "https://antideploy.com/api/v1"
}
```

| Status | Codes |
| --- | --- |
| 400 | `empty`, `bad_request`, `bad_archive`, `paths_mismatch`, `too_large`, `application_required` |
| 401 | `unauthorized` |
| 403 | `account_token_required` |
| 404 | `not_found` |
| 409 | `deploy_in_progress`, `name_taken` |
| 429 | `rate_limited`, `limit_exceeded` |

Match on `code`, not on the message text.

Two carry more than a message, and are worth reading rather than just relaying:
`too_large` lists every offending path in `files`, and `name_taken` returns the
`applicationId` you already have, so a script can use it instead of failing.

## Asynchronous by design

`POST /api/v1/deploy` returns `202` immediately with a `taskId`. Building and
releasing take a minute or two. Poll `/api/v1/deployments/{taskId}` until
`status` is `succeeded` or `failed`.

The poll response carries `warnings` and `hazards` as structured data. A
deploy can succeed and still not behave as its author expects; those fields
are how you find out.
